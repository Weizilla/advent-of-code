import { Solution } from "../solution";

class DayDAY extends Solution {
  constructor(example?: number, forcePrint: boolean = false) {
    super(DAY, YEAR, example, forcePrint);
  }

  part1(): number | string {
    return 0;
  }

  part2(): number | string {
    return 0;
  }
}

(new DayDAY(1)).run();
